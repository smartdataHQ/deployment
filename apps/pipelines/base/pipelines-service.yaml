apiVersion: v1
kind: Service
metadata:
  name: pipelines
  labels:
    app: pipelines
    tier: ingress
spec:
  type: ClusterIP
  ports:
    - port: 9999 3030
  selector:
    app: pipelines
    tier: ingress

