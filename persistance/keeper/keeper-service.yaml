apiVersion: v1
kind: Service
metadata:
  name: keeper
  labels:
    app: keeper
    tier: persistence
spec:
  type: ClusterIP
  ports:
    - port: 9000
      name: tcp
    - port: 8123
      name: http
    - port: 9181
      name: zookeeper
  selector:
    app: keeper
    tier: persistence
